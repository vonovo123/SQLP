
INDEX(E EMP_X1) INDEX(D DEPT_PK)

SELECT /*+LEADING(E D) USE_NL(D) INDEX(E) INDEX(D)*/
* 
FROM EMP E, DEPT D
WHERE E.DEPTNO = D.DEPTNO 
AND E.JOB = 'CLERK'
AND  (D.LOC = 'DALLAS' OR E.MGR = 7782);

SELECT * FROM DBMS_XPLAN.DISPLAY_CURSOR(NULL, NULL, 'ALLSTATS LAST -ROWS +OUTLINE +PREDICATE +ALIAS');

CREATE INDEX EMP_X1 ON EMP (JOB, DEPTNO);
CREATE INDEX EMP_X2 ON EMP (MGR, JOB);

CREATE INDEX DEPT_X1 ON DEPT (LOC);
CREATE INDEX DEPT_X2 ON DEPT (LOC, DEPTNO);

DROP INDEX DEPT_X2;
DROP INDEX PK_DEPT;


ALTER TABLE DEPT
ADD CONSTRAINT DEPT_PK PRIMARY KEY (DEPTNO);

ALTER TABLE EMP
ADD CONSTRAINT EMP PRIMARY KEY (EMPNO);


SELECT /*+ NO_SWAP_JOIN_INPUTS(D) USE_NL(D)  USE_CONCAT*/
* 
FROM EMP E, DEPT D
WHERE E.DEPTNO = D.DEPTNO 
AND E.JOB = 'CLERK'
AND  (D.LOC = 'DALLAS' OR E.MGR = 7782);

SELECT * FROM DBMS_XPLAN.DISPLAY_CURSOR(NULL, NULL, 'ALLSTATS LAST -ROWS +OUTLINE +PREDICATE +ALIAS');