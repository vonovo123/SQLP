32. CBO의 비용과 거리가 먼 것은?

a. 비용이란 기본적으로, sql 수행 과정에 수반될 것으로 예상되는 I/O 일량을 계산한 것이다.
b. 데이터베이스 Call 발생량도 옵티마이저의 중요한 비용 요소다.
데이터베이스 Call은 옵티마이저가 수립한 실행계획에 따라 SQL을 수행하는 과정에서, 또는 옵티마이저에게 실행계획을 수립해 달라고 요청하는 과정에서 발생한다.
c. 옵티마이저가 비용을 계산할 때, cpu 속도, 디스크 I/O속도 등도 고려할 수 있다.
d. 최신 옵티마이저는 I/O에 대한 CPU 연산 비용을 더해 비용을 계산한다.

33. 규칙기반옵티마이저가 사용하는 규칙으로 부적절한 것은?

a. 고객유형코드에 인덱스가 있으면, SQL에 인덱스를 사용한다.
b. 고객명에 인덱스가 있으면, 아래 SQL에 인덱스를 사용해 order by 소트 연산을 대채한다.
select \* from 고객 order by 고객명
c. 연령과 연봉에 인덱스가 하나씩 있으면 아래 sql에 연봉 인덱스를 사용한다. between 조건이 부등호 조건보다 스캔 범위가 작을 가능성이 높기 때문이다.
d. 직급에 인덱스가 있고, 직급 종류 개수가 5개 이상이면 인덱스를 사용한다.
select \* from 사원 where 직급 = '대리'
=> 컬럼 값의 종류개수 NDV는 CBP가 사용하는 대표적인 통게정보다.

34. 전체범위 최적화(All_ROWS) 방식의 옵티마이저 모드에 대한 설명으로 거리가 먼 것.

a. 쿼리의 최종 결과 집합을 끝까지 Fetch하는 것을 전제로, 시스템 리소스를 가장 적게 사용하는 것을 실행계획으로 선택.
b. 부분범위 취적화와 비교할 때, INDEX SCAN 보다 TABLE FULL SCAN 하는 실행계획을 더 많이 생성한다.
c. DML 문장은 옵티마이저 모드와 상관없이 항상 전체범위 최적화 방식으로 최적화된다.
d. 가장 빠른 응답속도를 목표로한다.
=> 전체범위 최적화는 빠른 Response Time 보다 Throughput 중심으로 최적화를 시행한다.

35. 비용기반 옵티마이저의 비용계산식.

- 선택도 : 1/NDV
- 카디널리티 : 총로우수 \* 선택도 = 총로우수 / NDV

36. 통계정보 수집 시 부적절한 고려사항

a. 시간/주기 : 부하가 없는 시간대에 가능한 빠르게 수집을 완료해야함.
b. 표본크기: 가능한 한 많은 양의 데이터를 읽어야함
=> 통계정보 수집은 시스템에 많은 부하를 주므로 대용량 테이블에서는 표본검사방식을 사용해야한다. 가능한 한 적은 양의 데이터를 읽고 전수 검사할 때의 통계치에 근접하도록 해야한다.
c. 정확성 : 표본 검사를 하더라도 전수 검사할 때의 통계치에 근접해야함
d. 안정송 : 데이터에 큰 변화가 없는데도 매번 통계치가 바뀌지 않아야함.

37. 동일 결과를 반환하는 sql 중 가장 효율적인 것

```sql


-- VIEW 멎이이 발생하면 함수호출 횟수가 증가한다
SELECT 상품코드, 당일매출, 전일매출, (당일매출 - 전일매출) * 100 / 당일매출증감율
FROM (
  SELECT 상품코드, ItemAmtFunc(상품코드, SYSDATE) 당일매출, ItemAmtFunc(상품코드, SYSDATE) 전일매출
  FROM 상품
  WHERE 상품분류코드 = '110'
)

-- ROWNUM을 명시해서 뷰머징을 방지하면 성능개선에 도움이된다.
SELECT 상품코드, 당일매출, 전일매출, (당일매출 - 전일매출) * 100 / 당일매출증감율
FROM (
  SELECT ROWNUM, 상품코드, ItemAmtFunc(상품코드, SYSDATE) 당일매출, ItemAmtFunc(상품코드, SYSDATE) 전일매출
  FROM 상품
  WHERE 상품분류코드 = '110'
)

-- 스칼라 서브쿼리의 캐싱효과를 활용하려했지만 상품코드가 PK이므로 캐싱효과가 없고 캐시 탐색하는 비용만 발생한다.
SELECT 상품코드, (SELECT ItemAmtFunc(상품코드, SYSDATE) FROM DUAL) 당일매출, (SELECT ItemAmtFunc(상품코드, SYSDATE - 1) FROm 전일매출)
(SELECT (ItemAmtFunc(상품코드, SYSDATE) - ItemAmtFunc(상품코드, SYSDATE - 1) * 100) / ItemAmtFunc(상품코드, SYSDATE) FROM DUAL ) 증감율
FROM 상품
WHERE 상품분류코드 = '110'
```

37. no_merge 힌트없이도 뷰 머징이 발생하지 않게 하는방법

```sql
select *
from (
  select rownu, emp_name, hire_date, sal, deptno
  from emp
  where deptno = 30
) x
where ...
```

38. 뷰 머징을 불가능하게 하는 경우가 아닌 것은

a. 뷰 안에 rownum 사용
b. 뷰 안에 group by 사용
=> group by 를 포함한 뷰는 자주 merging이 발생한다.
c. 뷰 안에 윈도우 함수 사용
d. 뷰 안에 union 연산자 사용

39. Sql에 대한 설명으로 적절한 것( 쿼리변한은 동작함)

```sql
select *
from (
  select ...
  from emp
  where job = 'CLEAR'
  union all
  select ...
  from emp
  where job = 'SALESMAN'
) v
where v.deptno = 30
```

a. Emp 테이블에 job + deptno로 구성된 인덱스를 만뜰면 job 조건만 인덱스 액세스 조건으로 사용되고, deptno는 인덱스 필터조건으로 처리된다.

b. emp 테이블 job에 단일 컬럼 인덱스를 만들면, 이 인덱스를 정상적으로 사용할 수 없다. 즉 INDEX RANGE SCAN 할 수 없다.

c. emp 테이블에 deptno + Job으로 구성된 인덱스를 만들면 두 컬렘에 대한 조건 모두를 인덱스 액세스 조건으로 사용할 수 있다.
=> deptno 조건이 인라인 뷰 안으로 파고 들어간다.(뷰머징)
d. emp 테이블 Deptno 단일 컬럼 인ㄷ게스를 만들면 이 인덱스를 정상적으로 사용할 수 없다.

41. sql을 처리할때 옵티마이저가 선택할 수 있는 옵션으로 부적절한 것은?

```sql
alter table 과금 add constraint 과금_Pk primary key (고객번호,과금월);
alter table 수납 add constraint 수납_Pk primary key (고객번호,수납일자);

select e1.고객번호, e1.과금액, e2.수납액, e1.과금액 - E2.수납액
from
(select 고객번호, sum(과금액) 과금액 from 과금 group by 고객번호) e1
, (
  select
  from 수납
  where 고객번호 = 10
  group by 고객번호
) e2
where e1.고객번호 = e2.고객번호
and e2.수납액 > 0
```

a. 두 인라인뷰를 풀어 고객번호 조인을 먼저 처리한후, 고객번호로 groupby 하면서 과금액과 수납액을 구하면 Cartesian Product가 발생해 결과가 틀릴 수 있다.
b. 인라인 뷰 e2에 있는 고객번호 = 10 조건을 e1에 전달해 과금pk 인덱스를 사용할 수 있도록 처리한다.
c. 메인 쿼리에 있는 수납ㅇ액 >0 조건을인라인 뷰 e2에 제공해 조인전 필터링이 일어나도록한다.
d. 사용자 작성한 sql 그대로 인라인 뷰를 따로 최적화한 후 조인한다.
