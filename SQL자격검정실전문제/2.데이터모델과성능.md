31. 성능데이터모델링에 대한 설명 중 부적절한 것

a 성능이 저하된 결과를 대상으로 데이터모델보다는 문제발생 시점의 sql을 중심으로 튜닝한다.
=> 문제 발생 시점의 SQL을 중심으로 튜닝하는 것은 성능 데이터모델링과 무관한다.

b. 데이터의 증가가 빠를 수록 성능저하에 따른 성능개선비용은 커진다.
c. 데이터모델은 성능을 튜닝하면서 변경될 수 있다.
d. 분석/설계 단계에서 성능을 고려한 모델링을 할 경우 성능 저하에 따른 rework 비용을 최소화 할 수 있다.

32. 33. 성능 데이터 모델링 수행 절차

a. 정규화를 정확히 수행한다.
b. 데이터베이스 용량을 산정한다.
c. 데이터베이스에서 발생되는 트랜잭션 유형을 파악한다.
d. 용량과 트랜잭션의 유형에 따라 '반정규화'를 실시한다.
e. 이력모델 조정, pk/fk 조정,슈퍼타입, 서브타입 조정등을 수행한다.

34. 성능데이터모델링 시 고려해야할 사항으로 부적절한 것

a.데이터모델링의 정규화는 항상 조회 성능저하를 유발하므로 반정규화 관점에서만 성능을 고려해 설계한다.
=> 정규화는 기본적으로 중복된 데이터를 제거함으로써 조회성능을 향상시킬 수 있다.

b.용량산정은 전체적인 데이터베이스에 발생되는 트랜잭션 유형과 양을 분석하는 자료가 되므로 성능데이터 모델링을 할 때 중요한 작업이된다.
c.물리적 데이터 모델링시 PK/FK의 컬럼의 순서조정, FK 인덱스 생성 등은 데이터 모델링 작업에 중요한 요소다.
d.이력데이터 처럼 대량으로 적재될 가능성이 높은 데이터는 성능을 고려해 설계한다.

35. 반정규화 된 엔터티로 정규화

보관금원장 엔터이에서 관서에 대한 정보가 반정규화 되어있어 관서정보를 조회할때 성능이 저하되고있다.

```sql
-- 보관금원장
-- #관서번호
-- #납부자번호
-- 관리점번호
-- 관서명
-- 상태
-- 관서등록일자
-- 직급명
-- 통신번호

-- 함수종속성 : [관서번호, 납부자번호] => [직급명, 통신번호]
-- 함수종속성 : [관서번호] => [관리점번호, 관서명, 상태, 관서등록일자]
```

a. 2차 정규화 - 정규화테이블[#관서번호, #납부자번호, 관리점번호, 관서명, 상태, 관서등록일자]
b. 3차 정규화 - 정규화테이블[#관서번호, #납부자번호, 관리점번호, 관서명, 상태, 관서등록일자]
c. 2차 정규화 - 정규화테이블[#관서번호, 관리점번호, 관리점번호, 관서명, 상태, 관서등록일자]
=> 합수종속성 규칙에 따라 [관서번호] => [관리점번호, 관서명, 상태, 관서등록일자 ]인 관서번호가 PK인 엔터티가 2차 정규화로 분리되야한다.
d. 3차 정규화 - 정규화테이블[#관서번호, 관리점번호, 관리점번호, 관서명, 상태, 관서등록일자]

---

- 제 1정규형 : 모든 속성은 하나의 값만 가진다.
- 제 2정규형 : 엔터티의 일반속성은 주식별자 전체에 종속되야한다.
- 제 3정규형 : 엔터티 일반속성간에는 서로 종속적이지 않아야한다.

36. 2차 정규화

```sql
-- 매각물건
-- #매각물건번호
-- 감정가액
-- 등록일자
-- 등록자

-- 일자별매각물건
-- #매각물건번호(FK)
-- #매각일자 <- 반정규화된 속성(매각기일)
-- 매각시간 <- 반정규화된 속성(매각기일)
-- 매각장소 <- 반정규화된 속성(매각기일)
-- 최저매각가격
-- 물건상태코드

-- 매각일자별매각내용
-- #매각일자
-- 총매각금액
-- 총유찰금액
-- 총유찰물건수
```

a. 1차 정규화가 필요한 엔터티로 매각기일과 일자별매각물건으로 1:M 관계가 될 수 있다.
b. 1차 정규화 필요한 엔터티로서 매각기일과 일자별매각물건으로 1:1 관계가 될 수 있다.
c. 2차 정규화가 필요한 엔터티로서 매각기일과 일자별매각물건으로 1:M 관계가 될 수 있다.
d. 2차 정규화가 필요한 엔터티로 매각기일과 일자별매각물건으ㅗㄹ 1:1 관계가 될 수 있다.

---

매각기일은 일자별로 매각이 시행되는 장소와 시간을 의미하는 것으로, 일자별매각물건 엔터티의 매각시간, 매각장소 속성은

주식별자 매각물건번호, 매각일자 중 매각일자에만 종속되기 때문에 2차 정규화의 대상이 된다.

그러므로 매각일자를 주식별자로 하고 매각시간과 매각장소 속성을 포함하는 매각기일 엔터티를 분리시킨다.

이때 매각기일 엔터티는 일자별매각물건의 주식별자 중 일부로 독립했기 때문에 1:M 관계가된다.

이와 같은 2차 정규화를 통해 특정 장소에서 이루어진 매각내역을 조회하고자할때

100만건의 매각내역 데이터를 모두 읽어 원하는 장소에 해당하는 인스턴스를 찾아 매각일자별로 그룹핑 후 매각일자별매각내역과 조인할 필요가 없어

매우 적은 수의 매각기일 엔터티에서 특정장소에 해당하는 매각일자들을 찾아 매각일자별 매각내역과 1:1 조인하면 되기 때문에 I/O를 현저히 감소시킬 수 있다.

37. 동일한 유형의 속성이 카럼단위로 반복되는 경우 정규화

전제조건 : 유형기능분류코드에 해당하는 속성들은 분포도가 양호하며, SQL WHERE절에서 각각 상수값으로 조건 입력될 수 있는 특징을 가진다.

```sql
-- 모델
-- #모델코드
-- 모델명
-- 제품류코드
-- 물품가
-- 출하가
-- A유형기능분류코드1
-- B유형기능분류코드2
-- C유형기능분류코드3
-- ...
```

a. 조회 조건이 유형기능분류코드에 따라 반복되는 그룹이 컬럼단위로 되어 있으므로 제 1정규형이다.

b. 유형기능분류코드에 대해 WHERE 절에 조건으로 들어오는 값이 있으므로 PK와 이에 대한 인덱스만 있으면 빠르게 수행될 수 있다.

c. 유형가능분류코드가 일반속성 안에서 반복적으로 속성으로 구분되어있기 때문에 이전종속을 수행해야 하는 제 2정규형이다.

d. 유형기능분류코드 각각에 대해 개별로 Index를 모두 생성할 경우 입력, 수정, 삭제 때 성능이 저하되므로 1차 정규화를 수행한 후 인덱스를 적용시키는 것이 낫다.

---

칼럼에 의한 반복적인 속성값을 갖는 형태는 속성의 원자성을 위배한 1차 정규화 대상이다.
이와 같은 반복적인 속성 나열 형태에서는 각 속성에 대한 or 연산자로 연결된 조건들이 사용되는데,
이 때 어느 하나의 속성이라도 인덱스에 없으면 한 번에 전체 데이터 스캔으로 처리되어 성능 저하가 나타날 수 있다.

또한 모든 반복 속성에 인댁스를 생성하면 검색 속도는 좋아지지만 CRUD 성능이 저하되므로 1정규화를 우선해야한다.

38. 1차 정규화

```sql
-- #물류센터코드
-- #재고일자
-- 월초재고수량
-- 장기재고1개월수량
-- 장기재고2개월수량
-- 장기재고3개월수량
-- 장기재고1개월주문수량
-- 장기재고2개월주문수량
-- 장기재고3개월주문수량
-- 장기재고1개월금액
-- 장기재고2개월금액
-- 장기재고3개월금액
-- 장기재고1개월주문금액
-- 장기재고2개월주문금액
-- 장기재고3개월주문금액
```

a. 1차 정규화가 필요한 엔터티로 일재고와 일재고상세로 1:M 관계가 된다.
b. 1차 정규화가 필요한 엔터티로 일재고와 일재고상세로 1:1 관계가 된다.
c. 2차 정규화가 필요한 엔터티로 일재고와 일재고상세로 1:M 관계가 된다.
d. 2차 정규화가 필요한 엔터티로 일재고와 일재고상세로 1:1 관계가 된다.

---

동일한 유형의 속성이 칼럼 단위로 반복되는 경우 1차 정규화의 대상이 된다.

1차 정규화를 통해 1:M 관계의 두개의 엔터티로 분리할 수 있다.

39. 1차 정규형, 2차 정규화

```sql
-- 수강지도
-- #학번
-- #과목코드
-- 성적
-- 지도교슈명
-- 학과명

-- 함수종속성
-- 1. 한번 || 과목번호 -> 성적
-- 2. 학번 -> 지도교수명
-- 3. 학번 -> 학과명
```

PK에 대해 반복되는 그룹이 존재하지 않으면(동일한 유형의 속성이 칼럼 단위로 반복되는 경우가 아니므로) 1차 정규형이다.
부분 함수종속(엔터티의 일반속성이 주식별자 전체에 종속되지 않는 경우) 2차 정규화의 대상이 된다.
중복속성에 대한 분리가 1차 정규화의 대상이된다. 로우단위의 중복, 칼럼 단위의 중복 모두 해당된다.

40. 반정규화 고려시 판단요소에 대한 적절한 설명

a. 반정규화 정보에 대한 재현의 적시성을 판단한다. 예를들어, 빌링의 잔액은 다수 테이블에 대한 다량의 조인이 불가피하므로 데이터 제공의 적시성 확보를 위한 필수 반정규화 대상이된다.

b. 탐색 대상 데이터의 크기로 판단한다. 왜냐하면 다량 데이터에 대한 인덱스를 활용한 탐색은 Random 처리의 특성으로 성능저하가 불가피하다.
=> 다량 데이터 탐색의 경우 인덱스가 아닌 파티션 및 데이터 클러스터링 등의 다양한 물리 저장 기법을 활용해 성능개선을 유도할 수 있다. 다만, 하나의 결과셋을 추출하기 위해 다량의 데이터를 탐색하는 처리가 반복적으로 빈번하게 발생한다면 이때는 반정규화를 고려해야한다.

c. RDBMS는 현재 레코드 기준으로 이전 또는 이후 위치의 레코드에 대한 접근이 원천적으로 불가능하므로 반정규화를 하지 않으면 해당 정보에 대한 데이터 접근이 불가하다.
=> 이전 또는 이후 위치의 레코드에 대한 탐색은 Window Function으로 접근 가능하다.

d. 반정규화 테이블은 집계 테이블에 국한하여 적용한다.
=> 집계 테이블 이외에도 다양한 테이블에 대하여 반정규화 테이블 적용이 필요할 수 있다.

41. 다음 중 하나의 테이블의 전체 컬럼 중 자주 이용하는 집중화된 컬럼들이 있을때 디스크 I/O를 줄이기 위해 해당 컬럼을 별도로 모아놓는 반정규화 기법은?

a. 컬럼추가 - 부분컬럼 추가
b. 컬럼추가 - 중복컬럼 추가
c. 테이블추가 - 중복테이블추가
d. 테이블추가 - 부분테이블추가

반정규화 기법은 테이블, 속성, 관계에 적용할 수 있다.

하나의 테이블의 전체 칼럼 중 자주 이용하는 집중화된 컬럼이 있을 때 디스크 I/O를 줄이기 위해 해당 칼럼들을 별도로 모으는 반 정규화 기법은 테이블추가 반정규화 기법 중에서

'부분테이블 추가'에 해당한다.

- 테이블의 반정규화

| 기법분류    | 반정규화 기법            |
| ----------- | ------------------------ |
| 테이블병합  | 1:1 관계 테이블병합      |
|             | 1:M 관계 테이블 병합     |
|             | 슈퍼/서브타입 테이블병합 |
| 테이블 분할 | 수직분할                 |
|             | 수평분할                 |
| 테이블추가  | 중복테이블추가           |
|             | 통계테이블 추가          |
|             | 이력테이블 추가          |
|             | 부분테이블 추가          |

- 컬럼의 반정규화

| 기법분류                          |
| --------------------------------- |
| 중복컬럼추가                      |
| 파생컬럼 추가                     |
| 이력테이블 칼럼추가               |
| PK에 의한 컬럼추가                |
| 응용시스템 오작동을 위한 컬럼추가 |

42. 컬럼에 대한 반정규화 기법으로 부적절한 것은

a.중복컬럼을 추가 - 조인감소를 위해 여러 테이블에 동일한 칼럼을 갖도록한다.
=> 조인 감소를 위해 여러 테이블에 동일한 컬럼을 갖도록 하는 기법은 컬럼의 반정규화 방법중 중복칼럼추가이다.

b.파생컬럼을 추가 - 조회 성능을 우수하게 하기 위해 미리 계산된 칼럼을 갖도록 한다.
=> 조회 성능 향상을 위해 미리 계산된 컬럼을 갖도록 하는 기법은 파생칼럼 추가기법이다.

c.Fk에 대한 속성 추가 - FK 관계에 해당하는 속성을 추가하여 조인성능을 높인다.

d.이력테이블에 기능컬럼 추가 - 최신값을 처리하는 이력의 특성을 고려해 기능성 칼럼을 추가한다.
=> 최신값을 처리하는 이력의 특성을 고려해 기능성 컬럼을 추가하는 기법은 이력테이블 컬럼추가기법이다.

43. 반정규화 기법

```sql
-- 주문
-- #주문번호

-- 주문목록
-- *주문번호
-- *제품번호

-- 제품
-- #제품번호
-- 단가

SELECT A.주문번호, SuM(C.단가)
FROM 주문 A, 주문목록 B, 제품 C
WHERE A.주문번호 = '2015-02-001'
AND A.주문번호 = B.주문번호
AND B.제품번호 = C.제품번호
GROUP BY A.주문번호
```

a.제품 엔터티에 단가를 합한 계산된 컬럼을 추가한다.
=> 제품 엔터티에 단가를 주문번호별로 합하는 것은 해당 제품이 여로 주문에 포함될 수 있기 때문에 특정 주문번호의 단가 합계금액을 갖고 잇을 수 없다.

b.주문목록 엔터티에 단가를 합한 계산된 칼럼을 추가한다.
=> 주문목록 엔터티에 주문번호별 단가 합계금액을 추가하게 되면 하나의 주문에 포함된 제품번호마다 동일한 합계 금액을 반복적으로 저장해야하므로 일관성 문제가 발생한다.

c.주문 엔터터에 단가를 합한 계산된 칼럼을 추가하도록한다.
=> 그러므로 주문 엔터티에 전체를 통합한 계산된 칼럼을 추가하는것이 한번에 데이터를 조회하는 가장 효과적인 반정규화 기법이다.

d. 제품 엔터티에 최근값 여부에 대한 칼럼을 추가하도록한다.
=> 제품 엔터티에 최근값 여부 칼럼을 추가하는 것은 단가 합계 금액을 빠르게 얻기 위한 반정규화와 무관하다.

44. 반정규화

- 공급자 데이터는 1000만건 이상의 대량 데이터를 가진 테이블이다.
- 공급자 테이블과 조회하는 전화번호, 메일주소, 위치는 자주 변경될 수 있으며 데이터를 조회할 때는 항상 최근에 변경된 값을 조회한다.

a. 공급자별로 최근에 변경된 전화번호, 메일주소, 위치와 공급자 이름을 같이 조회할 때 이 값들을 공급자 테이블에 반정규화로 갖고 있는 경우에 비해 조회성능이 감소하지 않는다.
=> 정규회된 테이블에서 최근에 변경된 값 만을 조회할 경우에도 과도한 조인으로 인해 성능이 저하된다.

b. 데이터를 조회할 때 과도한 조인으로 인해 성능이 저하될 수 있으므로 공급자 테이블에 가장 빈번하게 조회되는 값인 최근 변경값에 해당하는 전화번호, 메일주소, 위치를 반정규화하여 조회 성능을 향상시킬 수 있다.

c. 전화번호, 메일주소, 위치에 대한 가장 최근 변경값을 알 수 있도록 최신여부라는 속성을 추가하여 최근 값을 찾기 위한 조회 성능 저하를 예방할 수 있다.

d. 조회성능을 위해서는 하나의 테이블로 통합하여 전화번호, 메일주소, 위치 등이 변경될 경우 전체 속성이 계속 발생되는 이력의 형태로 설계된다.
이럴 경우 조회 성능은 향상되지만 테이블의 용량이 너무 커지는 단점이 있다.

45. 컬럼수가 많은 테이블에 대한 설명으로 적절한 것

a. 한 테이블에 많은 컬럼을 가지고 있으면서 조인이 발생되지 않아 여러 개 테이블일 때에 비해 성능이 항상 우수하다.

b. 로우체이닝이 발생할 정도로 한 테이블에 많은 컬럼이 존재할 경우, 조회성능저하가 발생할 수 있다. 한 테이블내에서 칼럼의 위치를 조정하면 디스크 I/O가 줄어들어 조회 성능이 향상된다.

c. 로우체이닝이 발생할 정도로 한 테이블에 많은 컬럼이 존재하면 조회성능저하가 발생한다. 트랜잭션이 접근하는 컬럼유형을 분석하여 1:1로 분리하면 디스크 I/O가 줄어 조회성능이 향상된다.

d. 로우체이닝이 발생할 정도로 한 테이블에 많은 컬럼이 존재하면 조회성능저하가 발생한다. 그러나 이를 분리할 경우 조인으로 인한 성능 저하가 더 심하게 나타날 수 있으므로 감수하는게 좋다.

---

한 테이블에 많은 컬럼이 존재할 경우, 데이터가 물리적으로 저장되는 디스크 상에 넓게 분포될 가능성이 커지므로 디스크 I/O가 대량으로 발생할 수 있고 이로 인해 성능 저하가 발생한다.

따라서 트랜잭션이 접근하는 컬럼유형을 분석해 자주 접근하는 컬럼들과 상대적으로 접근 빈도가 낮은 컬럼을 구분하여 1:1로 분리하면 디스크 I/O가 줄어들어 성능을 향상시킬 수 있다.

테이블 내에서 컬럼의 순서르 바꾸는 것은 데이터 주로 채워지는 컬럼을 앞 쪽에 위치시키고, 주로 Null 상태로 존재하는 컬럼들을 뒤쪽에 모아 로우의 길이를 어느정도 감소시킬 수 있지만

Null 상태이던 컬럼에 나중에 데이터가 채워지게 될 경우 더 만흔 로우 체인이 발생할 수 있다.

자주 사용되는 컬럼들이나 현시점에 주로 사용되는 컬럼을 한데 모으고, 사용빈도가 낮은 컬럼들이나 미래 시점에 사용될 것으로 예상되는 나머지 컬럼을 한데 모아 별도 관계 엔터티로 분리하는 설계수정을 고려해야한다.

---

#### 반정규화 대상에 대대 다른 방법으로 처리

- 지나치게 많은 조인이 걸려 데이터를 조회하는 작업이 기술적으로 어려운 경우 view 를 사용하면 해결할 수도 있다.
- 대량의 데이터처리나 부분처리에 의해 성능이 저하되는 경우 클러스터링을 적용하거나 인덱스를 조정함으로써 성능을 향상시킬 수 있다.
- 대량의 데이터는 PrimaryKey의 성격에 따라 부분적인 테이블로 분리할 수 있다. 즉 파티셔닝 기법이 적용되어 성능저하를 방지할 수 있다.
- 응용 단에서 로직을 구사하는 방법을 변경함으로써 성능을 향상시킬 수 있다/

---

46. 데이터 액세스 성능 향상을 위한 방법

하나의 테이블에 많은 양의 데이터가 저장되면 인덱스를 추가하고 테이블을 몇 개로 쪼개도 성능이 저하되는 경우가 많다.

이때 논리적으로는 하나의 테이블이지만 물리적으로는 여러 개의 테이블로 분리하여 데이터 액세스 성능도 향상시키고, 데이터 관리방법도 개선할 수 있도록 테이블에 적용하는 기법을

'파티셔닝' 이라고 한다.

47. 데이터 모델과 SQL문에 대해 개성해야할 상황

```sql
-- 긴급사건
# 긴급사건번호
사건명
발생일시

-- 특수사건
# 특수사건번호
사건명
발생일시

-- 일반사건
# 일반사건번호
사건명
발생일시

-- 위 세 테이블은 함께 조회하는 경우가 대부분이고 아직 시스템을 오픈하지 않았다.

SELECT 긴급사건번호, 사건명 FROM 긴급사건 WHERE 발생일시 = '20150905'
UNION ALL
SELECT 특수사건번호, 사건명 FROM 특수사건 WHERE 발생일시 = '20150905'
UNION ALL
SELECT 일반사건번호, 사건명FROM 일반사건 WHERE 발생일시 = '20150905'
```

a. Union all로 조회하면 정렬로 인해 성능이 저하되므로 union으로 조합하여 조회한다.
=> 개별 테이블을 모두 조회하는 트랜잭션이 대부분이라는 가정이 있으므로 Union all/Union의 경우 개별조회에 따른 시간소요와 이를 조합하는 성능저하가 발생한다.

b. 긴급사건, 특수사건, 일반사건을 하나의 테이블로 통합하고 PK를 사건분류코드 + 사건번호로 조합해 테이블을 구성한다
=> 따라서 하나의 테이블로 통합하고 PK 체계나 일반속성에 각 사건을 구분할 수 있도록 구분자를둔다.
c. 긴급사건, 특수사건, 일반사건 테이블을 개별로 유지하되 PK에 사건분류코드를 포함한다.
d. 세 개의 테이블을 그대로 유지하되 반정규화된 형태의 통합테이블을 하나 더 생성하여 조회 성능을 향상하도록한다.

48. 논리데이터모델의 슈퍼타입과 서브타입 데이터 모델을 물리적인 테이블 형식으로 변환할 때 가장 부적절한 것

a. 트랜잭션은 항상 전체를 대상으로 일괄 처리하는데 테이블은 서브타입별로 개별 유지하는 것으로 변환하면 Union 연산에 의해 성능이 저하된다.

b. 트랜잭션은 항상 서브타입 개별로 처리하는데 테이블은 하나로 통합하여 변환하면 불필요하게 많은 양의 데이터가 직접되어 있어 성능이 저하된다.

c. 트랜잭션은 항상 슈퍼 + 서브 타입을 함께 처리하는데 개별로 유지하면 조인에 의해 성능이 저하된다.

d. 트랜잭션은 항상 전체를 통합하여 분석처리하는데 하나로 통합되어 있으면 데이터 집적으로 인해 성능이 저하된다.
=> 트랜잭션은 항상 전체를 통합하여 분석 처리하는데 슈퍼-서브타입이 하나의 테이블로 통합되어 잇으면 하나의 테이블에 직접된 데이터만 읽ㅇ ㅓ처리할 수 있기 때문에 다른 형식에 비해 성능이 우수하다(조인감소)

49. 특정 테이블/쿼리 상태에서 최적의 SQL 패턴인것은

```sql
-- 현급출급기실정
-- PK인덱스는 설계된 순서 그대로 생성함
-- #거래일자
-- #사무소코드
-- #출급기번호
-- #명세표번호
-- 건수
-- 금액

SELECT 건수, 금액
FROM 현금출급기실정
WHERE 거래일자 BETWEEN '20140701' AND '20240702'
AND 사무소코드 = '000368'
```

a.사무소코드가 =로 상수값으로 들어왔고, 거래일자가 범위 BETWEEn조건이므로 거래일자 + 사무소코드 + 출급기번호 + 명세표 번호로 구성된 PK인덱스는 최적의 효율이다.
b.사무소코드가 =로 상수값으로 들어왔고, 거래일자가 범위 BETWEEn조건이므로 PK의 순서를 사무소코드 + 출급기번호 + 명세표번호 + 거래일자로 바꾸고 인덱스 생성하는 것이 유리하다.
c.사무소코드가 =로 상수값으로 들어왔고, 거래일자가 범위 BETWEEn조건이므로 PK 순서를 거래일자 + 출급기번호 + 명세표번호 + 사무소코드로 바꾸고 인덱스를 생성하는 것이 유리
c.사무소코드가 =로 상수값으로 들어왔고, 거래일자가 범위 BETWEEn조건이므로 PK 순서를 사무소코드 + 거래일자 + 출급기번호 + 명세표코드로 바꾸고 인덱스를 생성하는 것이 유리
=> 인덱스는 값의 범위에 따라 일정하게 정렬이 되어 있으므로 상수값으로 EQUAL 조건으로 컬럼이 가장 앞으로 나오고 범위조회 하는 유형의 컬럼이 다음에 오도록하는 것이 인덱스 탐색범위를 좁힐 수 있는 가장 좋은 방법이다.

50. 엔터티와 이 엔터티를 빈번하게 참조하는 SQL의 성능에 대한 설명으로 적절한 것

```sql
-- PK 인덱스가 존재하며 컬럼순서대로 인덱스가 생성되어 있고, 이 SQL이 트랜잭션의 대부분을 차지
-- 실적
-- #일자
-- #지사코드
-- #명세표번호
-- 금액
-- 건수

SELECT 건수, 금액
FROM 현금출급기실정
WHERE 일자 BETWEEN '20140701' AND '20240702'
AND 지사코드 = '000368'
```

a. 명세번호를 실적 테이블에 맨 처음 칼럼으로 옮기면 PK 인덱스의 이용 효율성이 향상된다.
b. 지사코드에 대한 입력 값이 EQUAL 조건으로 사용되므로 일자 컬럼의 앞에두면 인덱스 이용 효율이 높아진다.
=> '='로 들어온 조건에 해당하는 칼럼이 인덱스의 가장 앞쪽에 위치할 때 인덱스의 이용효율이 높다.
c. 일자가 SQL문장의 WHERE 절에 첫 번째 조건으로 나왔으므로 컬럼 순서에서도 첫번째에 위치하는 것이 효율적이다.
d. 일자, 명세번호, 지사코드로 컬럼 순서를 바꿔야 일자 범위에 대한 내용을 식별하고, 그 다음 지사코드를 찾기에 용이하다.

---

PK 순서를 결정하는 기준은 인덱스 정렬구조를 이해한 상태에서 인덱스를 효율적으로 이용할 수 있도록 PK 순서를 지정해야한다.

즉, 인덱스의 특징은 여러개의 속성이 하나의 인덱스로 구성되어 있을때 앞쪽에 위치한 속성의 값이 비교자로 있어야 효율이 좋다.

앞쪽에 위치한 속성이 가급적 = 아니면 최소한 BETWEEN <> 가 들어와야 인덱스를 이용할 수 있다.

51. 데이터모델에 표현된 FK에 대한 설명으로 적절한 것

```sql
-- 학사기준
-- #학사기준번호
-- 년도
-- 학기
-- 특이사항

-- 수강신청
-- #강의번호
-- *학번
-- 성명
-- 연락처1
-- 연락처2
-- 등록년도
-- 감면코드

-- 학사기분과 수강신청은 조인하여 정보를 조회할 업무가 많음
-- 학사기준과 수강신청은 1: N
-- 수강신청은 학사기준과 필수관계이고 학사기준은은 수강신청의 선택관계이다.
```

a. 학사기준번호는 부모 테이블에 이미 인덱스가 존재하기때문에 상속받아 생긴 수강신청에는 학사기준번호 컬럼에 대한 별도의 인덱스가 필요없다.

b. 학사기준번호는 부모 테이블에 이미 인덱스가 존재하나 수강신청과 조인에 의한 성능저하 예방을위해 상속받아 생긴 수강신청에도 학사기준번호 컬럼에 대한 별도의 인덱스가 필요하다.

C. 데이터모델에서는 관계를 연결하고 데이터베이스에 FK 제약조건 생성을 생략하는 경우에 학사기준번호에 대한 인덱스를 생성할 필요가없다.

d. 데이터모델에서는 관계를 연결하고 데이터베이스에 FK 제약조건 생성을 생략하는 경우에도 데이터의 조인관계가 필요하므로 학사기준번호에 대한 인덱스를 생성할 필요가있다.

---

엔터티간에 논리적 관계가 있는경우, 상호간 조인이 자주발생함을 의미한다.

때문에, 데이터베이스 상에서 DBMS가 제공하는 FK Constraint를 생성했는지 여부와 관계없이 조인 성능을 향상시키기 위한 인덱스를 생성해주는 것이 좋다.

FK CONSTRAINT는 데이터 모델 상의 표현된 논리적 관계에 따라 인스턴스 간의 일관성을 보장하기 위해 설계된 제약조건을 구현할 수 있도록 DBMS가 제공해주는 하나의 지원기능일 뿐이다.

52. 분산데이터베이스의 효율성 증대방안으로 틀린것.

a. 공통코드, 기준정보 등 마스터 데이터는 분산데이터베이스에 복제분산을 적용한다.
=> 공통코드, 기준정보 등과 같은 마스터 데이터를 한 곳에 두고 운영하는 경우 원격지에 접근이 빈번할 수록 실시간 업무처리 성능이 떨어지므로 분산환경에 복제분산하면 효율적이다.

b. 거의 실시간 업무적인 특성을 가지고 있을 때 분산 데이터베이스를 사용하여 구성할 수 있다.

c. 백업 사이트를 구성할 때 간단하게 분산기능을 적용해 구성할 수 있다.
=> 백업 사이트 구성에 대해서도 분산환경으로 구성하여 적용할 수 있다.

d. Global Single Instance를 구성할 때 분산데이터베이스를 하용하는 것이 효율적이다.
=> GSI는 한 개의 인스턴스 즉, 통합 데이터베이스 구조를 의미하므로 분산데이터베이스와 대치된다.
