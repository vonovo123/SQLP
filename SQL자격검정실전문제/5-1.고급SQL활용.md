64. null 허용 컬럼 연산

```sql
-- col1 col2, col3
--  10  20    null
--  15  null  null
--  50  70    20
```

a. select sum(col2) from tab1의 결과는 null이다.
b. select sum(col1 + col2 + col3) from tab1의 결과는 185이다.
c. select sum(col2 + col3) from tab1의 결과는 90이다.
d. select sum(col2) + sum(col3) from tab1의 결과는 90이다.

---

컬럼끼리 연산할 때 null을 포함하면 결과는 null이다.
레코드끼리 연산할때 null은 연산에서 제외된다.

65. sql과 실행계획 연결

```sql
--a
select * from emp order by deptno;

-- select statement
--    sort order by
--      table access full

--b
select distinct deptno from emp;
-- select statement
--    sort unique
--      table access full

--c
select deptno, count(*) from emp group by deptno;

-- select statement
--    sort aggregate
--      table access full

--d
select empno, ename, sal, avg(sal) over (partiton by deptno) from emp;
-- select statement
--    window sort
--      table access full
```

66. sort 오퍼레이션을 포함하지 않는 sql

a. select \* from emp order by ename
b. select \* from where deptno = 20 union all select \* from emp where deptno = 30
c. select empno, ename, rank() over(partition by deptno order by sal desc) from emp
d. select \* from dept d, emp e where d.deptno = e.deptno option (force order ,merge join)

---

Union all은 정렬 오퍼레이션을 발생시키지 않는다.

67. union 대신 union all을 사용해도 결과가 동일한 질의

```sql
-- emp
-- #empno
-- * ename
-- * deptno
-- job
-- mgr
-- sal
```

select empno, job, mgr from emp where deptno = 10
union
select empno, job, mgr from emp where deptno = 20

## pk 컬럼을 포함하기 때문에 모든 레코드가 완전히 배타적이다

두 집합이 서로 베타적이어야함.
위 아래 각 집합에 중복 값이 없어야함

68. 수행빈도가 높은 쿼리의 튜닝방안

```sql
create index 게시판_idx on 게시판(작성일시, 게시판구분)

select *
from (
  select *
  from 게시판
  where 게시판구분 = :gubun
  and 작성일시 >= trunc(sysdate) - 1
  order by 작성일시, 작성자명
)
where rownum <= 100
```

a. rownum 조건을 인라인 뷰 안에서 사용한다.
=> 결과집합이 달라진다.
b. 게시판 구분의 데이터 분포를 고려해 인덱스를 [작성일시 + 작성자명] 순으로 변경할지 검토한다.
c. 데이터 분포를 고려해 인덱스를 [게시판구분 + 작성일시] 순으로 변경할지 검토한다. 최종 결정 시 다른 SQL에 미치는 영향도도 검토해야한다.
d. 작성자명이 데이터 정렬 순서로서 의미있는 것인지 현업 사용자에게 문의한 후 불필요하면 제거한다.

69. 이력조회 쿼리

```sql
-- 전체 고객을 대상으로 2010년 12월 4일자 이력 조회
-- 고객     1 - 1n  고객변경이력
-- #고객ID          #고객id
-- *고객명           #변경순번
-- *고객등급          *변경일자
-- *전화번호          *고객등급
-- 주소              *전화번호
-- 자녀수            주소
-- 직업              자녀수
--                  직업

-- RANDOM ACCESS 방식으로 전체 고객 대상시 비효율이 있음
select b.고객ID, b.변경순번, b.전화번호, b.주소, b.자녀수, b.직업, b.고객등급
from 고객 a, 고객변경이력 b
where b.고객id = a.고객id
and b.변경순번 = (
  select max(변경순번)
  from 고객변경이력
  where 변경일자 <= '20101204'
  and a.고객id = 고객id
)


-- 동일 테이블을 두 번 조회하는 비효율
select *
from
(
  select 고객id, max(변경순번) 변경순번
  from 고객변경이력
  where 변경일자 <= '20101204'
  group by 고객ID
) a
, 고객변경이력 b
where a.고객id = b.고객id
and a.변경순번 = b.변경순번

select *
from
(
  select 고객id, 변경순번, rank() over(partition by 고객id order by 변경순번 desc) rnum
  , 전화번호, 주소, 자녀수, 직업, 고객등급
  from 고객변경이력
  where 변경일자 <= '20101204'
)
where rnum = 1


--ㅅTOP-N 쿼리 알고리즘이 작동하지 않아 3번 방식에 비해 소트부하가 더 크다다
select *
from
(
  select 고객id, 변경순번, max() over(partition by 고객id order by 변경순번 desc) 변경순번2
  , 전화번호, 주소, 자녀수, 직업, 고객등급
  from 고객변경이력
  where 변경일자 <= '20101204'
)
where 변경순번 = 변경순번2
```

70. insert 트랜잭션

바인드변수의 입력값에 따라 선택적으로 주식월별시세 또는 선물월별시세 테이블에 입력한다.
실행 정보와 같이 바인드 변수를 입력하고 쿼리를 100번과 200번 세션에서 순차적으로 수행했을때 200 세션의 상태는?

```sql
insert /*+append*/ all
when :v_주식선물구분 = '주식'
then into 주식월별시세(종목코드, 거래일자, 종가)
when :v_주식선물구분 = '선물'
then into 선물월별시세(종목코드, 거래일자, 종가)
select a.종목코드
, :v_기준일자 as 거래일자
, avg(a.종가) as 종가
from 주식일별시세 a
where :v_주식선물구분 = '주식'
and a.거래일자 between add_months(:v_기준일자, -1) and :v_기준일자
Group by a.종목코드
union all
select a.종목코드
, :v_기준일자 as 거래일자
, avg(a.종가) as 종가
from 주식일별시세 a
where :v_주식선물구분 = '선물'
and a.거래일자 between add_months(:v_기준일자, -1) and :v_기준일자
Group by a.종목코드
```

---

INSERT ALL 구분에 Append 힌트를 사용하면 모든 테이블에 Exclusive 모드의 TM락이 설정된다.

71. 오라클 nologging 옵션과 append 힌트의 성능개선 효과 원리

```sql
alter table 수납 nologging;
insert /*+append*/ into 수납
select  * from 수납_임시
where 수납일시 between ...
```

a.Lock을 사용하지 않고 빠르게 입력한다.
=> append 모드로 insert하면 Exclusive 모드 테이블 Lock이 걸린다.
b.Redo와 Undo 발생량을 최소화한다.
c.데이터입력이 가능한 빈 블록을 찾기 위해 Freelist를 조회하지 않는다.
d.버퍼캐시를 거치지않고 데이터를 세그먼트 HWM 뒤쪽에 순차적으로 입력한다.

72. DML 튜닝방안으로 부적절한 것.

a. 대량 데이터에 대해 작업할 경우 인덱스를 Unusable 상태로 변경하고 작업한 후에 인덱스를 재생성하는 것이 빠를 수 있다.
b. 대량의 데이터를 빠르게 UPDATE 하기 위해 테이블을 nologging 모드로 변경하고 작업을 시작한다.
=> nologging 모드는 Insert 에만 작동한다.
c. 오라클의 수정가능 조인 뷰는 키-보존 테이블에만 입력, 수정, 삭제가 허용된다.
d. 대량 데이터에 대한 UPDATE나 DELETE 문의 경우 기존 데이터를 임시 테이블에 저장하고 테이블을 Truncate한 후 임시 테이블을 이용해 다시 입력하는 것이 빠르다.

73. sql에 대한 설명으로 부적절한 것

```sql
--(a)
UPDATE 급여지금 T
  SET T.월급여 = (
    SELECT S.월급여
    FROM 사원 S
    WHERE S.사원번호 = T.사원번호
  )
WHERE T.급여월 = '201101'
AND EXISTS (
  SELECT 1
  FROM 사원 S
  WHERE S.사원번호 = T.사원번호
  AND S.부서코드 = '30'
)
--(b)
MERGE INTO 급여지급 T
USING(
  SELECT S.사원번호, S.월급여
  FROM 사원 S
  WHERE S.부서코드 = '30'
) S
ON (
  T.급여월 = '201101'
  AND S.사원번호 = T.사원번호
)
WHEN MATCHED THEN
UPDATE SET T.월급여 = S.월급여
```

a. a와 b의 수정되는 데이터 건수는 같다.
b. 둘 다 nl방식으로 조인한다면, (a)가 (b)보다 사원테이블의 반복 엑세스가 많아 발생하는 I/O도 많다.
c. 둘다 해시 방식으로 조인하면, 아무리 처리량이 많아도 성능에 차이가 없다.
=> (A)의 서브쿼리를 nl세미조인방식으로 변경해도 조인에 참여하는 테이블이 더 많으므로 (B)보다 I/O가 더 많이 발생한다.
d. (b)는 insert 처리도 같이 하도록 문장을 재구성할 수 있다.

74. 야간에 단독으로 수행되는 대용량 배치 프로그램 속도 향상 튜닝법

a. 인덱스를 제거했다가 작업 완료 후 다시 생성한다.
b. 수정 가능 조인뷰나 merge 문을 활용한다.
c. oracle이라면 update 문을 수행하기전에 nologging 모드로 변경한다.
=> 오라클에서 nologging 기능은 insert 문자엥만 효과가있다.
d. sql server라면 최소 로깅모드 insert기능을 활용한다.

75. 테이블 또는 인덱스를 파티셔닝하는 이유로 부적절한 것.

a.가용성 향상
b.저장효율 개선
=> 테이블이나 인덱스를 파티셔닝하면 저장 효율은 오히려 나빠진다
c.조회성능 개선
d.경합분산

76. 다음 중 파티션에 대한 설명으로 가장 부적절한 것은?

a. 파티션은 애플리케이션에 투명하다. 즉, 파티션되지 않은 테이블을 새롭게 파티셔닝하더라도 쿼리는 수정하지 않아도 된다.
b. 기존에 사용되던 인덱스를 파티셔닝하면 그 인덱스를 액세스하던 일부 SQL 성능이 오히려 느려진다.
c. 파티션 컬럼에 대한 검색조건을 상수 값으로 제공해야 partition-prunning이 작동한다.
=> 파티션 컬럼에 대한 검색조건을 바인드 변수로 제공해도 partition-prunning은 작동한다.
d. 파티션 단위로 인덱스를 재생성할 수 있다.

77. 블록I/O 측면에서 비효율이 없는 SQL

```sql

create table 주문 (
  고객번호 varchar2(12), 주문일자 varchar2(8), 주문시각 varchar2(6)...
)
partition by range (주문일자)(
  partition m201101 values than('20110201')
  ,partition m201102 values than('20110301')
  ,partition m201103 values than('20110401')
  ,partition m201104 values than('20110501')
  ,partition m201105 values than('20110601')
  ,partition m201106 values than('20110701')
  ,partition mmmxaval values than(maxvalue)
)

--d번 항목 일자 테이블에 대한 블록 I/O는 소량이므로 무시한다.
--a
select * from 주문 where 주문일자 between '20110101' and '20110331'
--b
select * from 주문 where 주문일자 between '20110101' and '20110131'
union all
select * from 주문 where 주문일자 between '20110201' and '20110228'
union all
select * from 주문 where 주문일자 between '20110301' and '20110331'
--c
-- 파티션 키 컬럼을 가공했으므로 모든 파티션을 Full Scan 한다.
select * from 주문 where substr(주문일자, 1, 6) in ('201101', '201102', '201103')
--d
-- 주문 테이블이 월단위로 파티션돼 있는데 일 단위로 조회 조건을 제공하므로 각 월에 속한 일자개수만큼 파티션 Full Scan을 반복하게 된다.
select/*+ordered use_nl(b)*/ b.*
from 일자 a, 주문 b
where a.일자 between '20110101' and '20110331'
and b.주문일자 = a.일자
```

78. Local Prefixed 파티션 인덱스 생성

```sql
create table 거래 (고객번호 varchar2(10), 종목코드 varchar2(20), 거래일시 date, ...)
partition by range (거래일자) (
  partition p2010 values less than(to_date('20110101', 'yyyymmdd'))
  ,partition p2011 values less than(to_date('20120101', 'yyyymmdd'))
  ,partition p2012 values less than(to_date('20130101', 'yyyymmdd'))
  ,partition p2013 values less than(to_date('20140101', 'yyyymmdd'))
  ,partition pmax values less than(maxvalue)
)

create index 거래_N1 on 거래(거래일시) local;
```

---

local prefixed 파티션 인덱스는 인덱스 선두 컬럼이 팥티션키인 경우를 말한다.

79. 파티션 테이블에 적절한 파티션 인덱스

```sql
create table 거래 (
  계좌번호 number, 상품번호 varchar2(6)
  , 거래일자 varchar2(8), 거래량 number, 거래금액 number
) partition by range(거래일자) (
  partition p1 values less then('20110101')
  ,partition p2 values less then('20120101')
  ,partition px values less then(maxvalue)
);

-- GLOBAL PREFIXED
create index 거래_idx1 on 거래(거래일자, 상품번호) Global
partition by range(거래일자) (
  ...
)
-- LOCAL NONPREFIXED
create index 거래_idx02 on 거래(계좌번호, 거래일자) LOCAL;
```

---

Global 파티션 인덱스인 거래\_IDX1의 파티션 키는 거래일자이고 파티션 키가 인덱스 선두 컬럼이므로 Prefixed 파티션이다.
거래\_IDX2는 Local 인덱스이므로 파티션 키는 거래일자가 된다. 파티션 키가 인덱스 선두 컬럼이 아니므로 Nonprefixed 파티션이다.

80. 배치프로그램 튜닝방안으로 부적절한 것.

a. 시스템 사용자의 업무시간이 종료되자마자, 동시에 수행 가능한 모든 배치 프로그램을 집중적으로 수행하여 총 소요시간을 단축한다.
=> 같은 시간대에 수많은 프로그램이 집중적으로 수행되면 총 수행시간이 더 늘어난다. 자원과 lock에 대한 경합이 발생하면서 프로세스가 실제 일한 시간보다 대기하는 시간이 더 많아지기 때문이다.
b. 개별프로그램 측면에서도 최초 응답속도보다는 전체 처리속도 최적화를 목표로 설정해야한다.
c. 파티션과 병렬처리르 적절히 활용한다.
d. 여러 배치 프로그램에서 공통적으로 사용하는 집합을 정의해 이를 임시테이블로 생성한다.

81. 다음 중 오라클 병렬 프로세싱에 대한 설명으로 부적절한 것은.

a. 일련의 처리 과정이 모두 병렬로 진행되도록 해야 병렬 효과를 극대화할 수 있다.
b. 옵티마이저는 병렬 프로세스 간 통신량을 최소화하려고 노력한다.
c. 병렬 dml시, exclusive 모드 tm lock이 걸려 해당 테이블을 갱신하는 다른 트랜잭션이 모두 블록킹되므로 주의해야한다.
d. 병렬 update 는 Redo Log를 생성하지 않는다.
=> 테이블 nologging 모드로 바꾸면 Redo log가 생성되지 않도록 할 수 있지만, 이 기능은 append 또는 parallel 힌트를 사용해 Direct Load Insert 할 때만 작동한다.

82. 성능향상을 위한 병렬쿼리 힌트

- 주문테이블은 주문일자 기준으로 Range 파티셔닝, 고객번호 기준으로 Hash 서브 파티셔닝.
- 고객 테이블은 비파티션 테이블
- 등록 고객 수는 100만명, 월 평균 주문 레코드는 2천만건.

```sql
select /*+ordered use_hash(o) parallel(c 2) parallel(o 2)*/
o.고객번호, sum(o.주문금액), min(c.등급코드)
from 고객 c, 주문 o
where o.고객번호 = c.고객번호
and o.주문일자 between '20100901' and '20100930'
group by o.고객번호
```

---

\- partition partiton wise join

조인하는 두 테이블 중 한쪽만 파티셔닝 되어있고 한쪽은 비파티션 테이블일 경우 Partial Partition-Wise Join 가능
단, 파티션 키가 조인컬럼이어야함

\- PQ_DISTRIBUTE(INNER, partition, NONE)

PARTIAL PARTITION WISE JOIN 으로 유도할 때 사용하며, outer 테이블을 inner 테이블 파티션 기준에 따라 파티셔닝 하라느 뜻이다. 당연히 inner 테이블이 조인 키 칼럼에 대해 파티셔닝돼 있을 때만 작동한다.
